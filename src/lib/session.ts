/**
 * Estado de sesión en memoria (demo, sin backend).
 * Reemplazar por auth real (Supabase) en la Fase 1.
 */
export const session = {
  /** Si el usuario ya "inició sesión" en esta corrida. */
  authed: false,
};
